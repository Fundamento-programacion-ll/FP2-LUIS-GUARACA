var arreglo;
var buscarusuarios = (arreglo,usuarioBuscar)=>{
    var usuarioEncontrado = arreglo
    .filter((usuario.nombre === usuarioBuscar
        ));
        console.log(usuarioEncontrado);
};
buscarusuarios(arreglo,'luis');

